type: custom:apexcharts-card
graph_span: 31d
span:
  start: month
stacked: true
update_interval: 1m
header:
  show: true
  title: Besparing deze maand
  show_states: true
  colorize_states: true
apex_config:
  chart:
    toolbar:
      show: false
  annotations:
    yaxis:
      - y: 0
        borderColor: "#888888"
        strokeDashArray: 0
        borderWidth: 1
  xaxis:
    tooltip:
      enabled: false
    type: datetime
    tickAmount: 10
    labels:
      formatter: |
        EVAL:(value) => {
          const d = new Date(value);
          const base = new Date();
          const sameMonth =
            d.getMonth() === base.getMonth() &&
            d.getFullYear() === base.getFullYear();

          if (!sameMonth) return '';
          return d.getDate().toString();
        }
  tooltip:
    x:
      formatter: |
        EVAL:(value) => {
          const d = new Date(value);
          const s = d.toLocaleDateString('nl-NL', {
            weekday: 'long',
            day: 'numeric',
            month: 'long',
            year: 'numeric',
          });
          return s.charAt(0).toUpperCase() + s.slice(1);
        }
    y:
      formatter: |
        EVAL:(value, { seriesIndex, dataPointIndex, w }) => {
          const now = Date.now();
          const x = w?.globals?.seriesX?.[seriesIndex]?.[dataPointIndex];
          if (typeof x === 'number' && x > now) return '';

          return '€ ' + Number(value).toFixed(2);
        }
series:
  - entity: sensor.quatt_home_battery_savings_month
    name: Thuisbatterij
    type: column
    stack_group: savings
    color: "#27ae60"
    float_precision: 2
    show:
      in_chart: true
      in_header: false
      legend_value: false
    data_generator: |
      const raw = entity.attributes.savings_json;
      if (!raw) return [];

      let data;
      try {
        data = JSON.parse(raw);
      } catch {
        return [];
      }

      const points = data.result?.timeseries || [];
      return points.map(p => [
        new Date(p.startAt).getTime(),
        Number(((p.homeBatterySavingsCentsInclVat || 0) / 100).toFixed(2)),
      ]);
  - entity: sensor.quatt_home_battery_savings_month
    name: Onbalans
    type: column
    stack_group: savings
    color: "#3498db"
    float_precision: 2
    show:
      in_chart: true
      in_header: false
      legend_value: false
    data_generator: |
      const raw = entity.attributes.savings_json;
      if (!raw) return [];

      let data;
      try {
        data = JSON.parse(raw);
      } catch {
        return [];
      }

      const points = data.result?.timeseries || [];
      return points.map(p => [
        new Date(p.startAt).getTime(),
        Number(((p.imbalanceSavingsCentsInclVat || 0) / 100).toFixed(2)),
      ]);
  - entity: sensor.quatt_home_battery_savings_month
    name: Zonne-energie
    type: column
    stack_group: savings
    color: "#f4d03f"
    float_precision: 2
    show:
      in_chart: true
      in_header: false
      legend_value: false
    data_generator: |
      const raw = entity.attributes.savings_json;
      if (!raw) return [];

      let data;
      try {
        data = JSON.parse(raw);
      } catch {
        return [];
      }

      const points = data.result?.timeseries || [];
      return points.map(p => [
        new Date(p.startAt).getTime(),
        Number(((p.solarSavingsCentsInclVat || 0) / 100).toFixed(2)),
      ]);
  - entity: sensor.quatt_home_battery_savings_month
    name: totaal
    unit: €
    color: "#ffffff"
    show:
      in_chart: false
      in_header: true
      legend_value: false
    float_precision: 2
    data_generator: |
      const raw = entity.attributes.savings_json;
      if (!raw) return [];

      let data;
      try {
        data = JSON.parse(raw);
      } catch {
        return [];
      }

      return [[Date.now(), (data.result?.aggregated?.totalSavingsCentsInclVat || 0) / 100]];
  - entity: sensor.quatt_home_battery_savings_month
    name: thuisbatterij
    unit: €
    color: "#27ae60"
    show:
      in_chart: false
      in_header: true
      legend_value: false
    float_precision: 2
    data_generator: |
      const raw = entity.attributes.savings_json;
      if (!raw) return [];

      let data;
      try {
        data = JSON.parse(raw);
      } catch {
        return [];
      }

      return [[Date.now(), (data.result?.aggregated?.homeBatterySavingsCentsInclVat || 0) / 100]];
  - entity: sensor.quatt_home_battery_savings_month
    name: onbalans
    unit: €
    color: "#3498db"
    show:
      in_chart: false
      in_header: true
      legend_value: false
    float_precision: 2
    data_generator: |
      const raw = entity.attributes.savings_json;
      if (!raw) return [];

      let data;
      try {
        data = JSON.parse(raw);
      } catch {
        return [];
      }

      return [[Date.now(), (data.result?.aggregated?.imbalanceSavingsCentsInclVat || 0) / 100]];
  - entity: sensor.quatt_home_battery_savings_month
    name: zonne-energie
    unit: €
    color: "#f4d03f"
    show:
      in_chart: false
      in_header: true
      legend_value: false
    float_precision: 2
    data_generator: |
      const raw = entity.attributes.savings_json;
      if (!raw) return [];

      let data;
      try {
        data = JSON.parse(raw);
      } catch {
        return [];
      }

      return [[Date.now(), (data.result?.aggregated?.solarSavingsCentsInclVat || 0) / 100]];
